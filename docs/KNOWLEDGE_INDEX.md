# Knowledge Index — tower-defense

> "Accessed knowledge": the onboarding map agents read before changing code.
> Generated by `harness-cli knowledge`. Key Technologies, How to Run, Top-Level
> Structure and Key Subdirectories are regenerated each run; Purpose, Key
> Concepts and per-entry descriptions are authored and preserved between the
> markers.

## Purpose

<!-- KNOWLEDGE:PURPOSE:BEGIN -->

Repository này dùng để xây dựng game Auto-battler Tower Defense theo
`docs/TECHNICAL_SPEC.md`. Hiện repo đã có nền M0 bằng TypeScript/Vite/Canvas và
M1 render/data: fixed-step loop, seeded RNG, isometric grid, click-to-tile
selection, typed content data, static unit placement và deterministic depth
sorting. M2 combat lõi đã có pure simulation cho enemy spawn, flow-field
movement, nearest-target attacks, armor mitigation, enemy death và HUD combat
progress. M3 thêm run state setup/combat/result, deterministic shop,
buy/place/sell từ shop và bench, cùng browser controls để chơi qua một wave.

<!-- KNOWLEDGE:PURPOSE:END -->

## Key Technologies

- SQL
- Node.js
- TypeScript
- npm
- Prettier
- EditorConfig
- Markdown

## How to Run

- `npm run build` — build
- `npm run test` — test
- `npm run dev` — dev

## Top-Level Structure

- `AGENTS.md` — Quy tắc làm việc bắt buộc cho agent trong repo.
- `README.md` — Tên repo và điểm vào mô tả ngắn.
- `_harness/` — Runtime vận hành Harness, CLI, schema, docs và skills.
- `docs/` — Tài liệu sản phẩm, spec kỹ thuật, decisions và story packets.
- `index.html` — Browser entrypoint mount app Vite vào `#app`.
- `package-lock.json` — Lockfile npm cho dependency resolution lặp lại được.
- `package.json` — Scripts dev/build/test và dependency TypeScript/Vite/Vitest.
- `src/` — Mã nguồn ứng dụng browser, core utilities, platform và render.
- `tests/` — Vitest unit tests cho core deterministic behavior.
- `tsconfig.json` — Cấu hình TypeScript strict cho source và tests.

## Key Subdirectories

- `_harness/bin/` — Chứa executable `harness-cli`.
- `_harness/docs/` — Tài liệu tham chiếu sâu cho workflow, CLI, audit và
  maturity.
- `_harness/schema/` — Migration SQLite cho durable layer của Harness.
- `_harness/skills/` — Skill nội bộ dùng theo trigger của Harness.
- `docs/decisions/` — Decision records bền vững khi đổi kiến trúc/API/security.
- `docs/product/` — Product contract được xé nhỏ từ spec và story.
- `docs/stories/` — Story packets, backlog và kế hoạch triển khai theo epic.
- `src/core/` — Core engine utilities thuần như fixed-step loop và seeded RNG.
- `src/data/` — Typed JSON-style gameplay definitions for units, traits, enemies
  and the first level.
- `src/math/` — Math helpers cho coordinate conversion, grid bounds và
  flow-field pathing.
- `src/platform/` — Browser/platform wiring như canvas setup và resize.
- `src/render/` — Canvas 2D rendering cho board, tiles và HUD.
- `src/sim/` — Pure simulation-facing helpers for static board actor creation,
  M2 combat world updates, and M3 run/shop state.

## Key Concepts

<!-- KNOWLEDGE:CONCEPTS:BEGIN -->

- Product Delta: thay đổi code, tests, data, render, UI hoặc product docs của
  game.
- Story Packet: tài liệu story-sized phải có trước khi sửa code
  normal/high-risk.
- Durable Layer: trạng thái vận hành nằm trong `_harness/harness.db`, thao tác
  qua `_harness/bin/harness-cli`.
- Trace và Verification Gate: mọi thay đổi cần lệnh kiểm chứng đã chạy và trace
lưu lại kết quả. Xem thuật ngữ đầy đủ trong `_harness/docs/GLOSSARY.md`.
<!-- KNOWLEDGE:CONCEPTS:END -->
